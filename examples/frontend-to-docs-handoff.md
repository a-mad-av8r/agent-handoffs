# Frontend to Docs Handoff

```bash
./scripts/handoff-create \
  --from frontend \
  --to docs \
  --summary "Settings page copy changed" \
  --next "Update the user guide screenshots" \
  --verify "Guide mentions the new Save behaviour"
```

The receiving agent claims it before acting. Completion means the verification
step was performed, not merely that the message was read.
