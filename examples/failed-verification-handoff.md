# Failed Verification Handoff

A failed handoff is useful project memory:

```bash
./scripts/handoff-fail hnd_...
```

Failure should mean the receiving agent tried to verify the work and found a
blocking mismatch. It should not disappear into chat history.
